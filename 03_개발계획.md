# Fridge Guardian AI — 단계별 개발 계획서

> 개발자가 이 문서만 보고 순서대로 진행할 수 있도록 작성.
> 각 단계는 **목적(왜) → 방법(어떻게) → 세부 작업 → 산출물 → 완료 기준(DoD)** 구조.
> 스키마·코드는 "방향과 구조 파악용" 수준으로만 표기(상세 구현은 담당 개발자 재량).

- 문서 버전: v1.0
- 작성일: 2026-07-02
- 관련 문서: [01_기획서.md](01_기획서.md) · [02_기술아키텍처.md](02_기술아키텍처.md)
- 제약: **운영 비용 $0** (무료 티어 + NVIDIA NIM), **PWA(스토어 업로드 불필요)**

---

## 0. 문서 읽는 법 & 전체 지도

### 마일스톤 개요
| 마일스톤 | 단계 | 목표 | 기능 |
|----------|------|------|------|
| **M0** | S0 | 개발 기반 구축 | 리포·인프라·인증 |
| **M1 (MVP 코어)** | S1~S4 | "영수증 → 재고 → 임박 알림 → 레시피" 한 흐름 완성 | F1~F5 |
| **M2 (웹 차별화)** | S5~S7 | 공유·리포트·공유URL로 웹 강점 실현 | F6~F9 |
| **M3 (고도화)** | S8~S9 | 음성·이미지·학습 루프 | F10~F13 |

### 의존 순서 (선행 → 후행)
```
S0 기반 ─┬─ S1 영수증 등록(F1) ─── S2 소비기한(F2) ─┬─ S3 알림(F3)
         │                                          └─ S4 레시피(F4/F5)
         └─ (S1~S4 완료 후) ── S5 공유(F6) / S6 리포트(F7) / S7 공유URL(F8·F9)
                                          └─ S8 음성(F10) / S9 이미지·학습(F11~F13)
```
> **핵심 원칙**: M1은 "가로로 얇게" — 각 단계가 앞 단계의 산출물을 실제로 소비해 **S4 종료 시점에 데모 가능한 end-to-end 흐름**이 나오게 한다.

---

# M0 · 개발 기반

## S0. 프로젝트 셋업 & 인프라 골격

**목적** — 이후 모든 기능이 올라탈 공통 토대(리포 구조, 배포 파이프라인, 인증, DB 연결)를 먼저 세워 기능 개발 중 인프라로 막히지 않게 한다.

**방법** — 무료 티어 서비스를 계정 연결하고, 프런트/백엔드 최소 배포를 먼저 성공시켜(Hello World가 실제 URL로 뜨는 상태) "배포 가능한 뼈대"부터 확보한다.

**세부 작업**
- 모노레포 구조 생성
  ```
  fridge-guardian/
   ├─ apps/web/        # React + Vite PWA
   ├─ apps/api/        # FastAPI
   ├─ packages/shared/ # 공용 타입(품목·재고 스키마)
   └─ .github/workflows/  # CI + cron
  ```
- Supabase 프로젝트 생성 → Postgres/Auth/Storage 활성화, 로컬 개발용 `supabase` CLI 연결
- 프런트: Vite + `vite-plugin-pwa`로 PWA 스캐폴딩 → Cloudflare Pages 배포 연결
- 백엔드: FastAPI 최소 앱 → Fly.io 배포 연결, `/health` 엔드포인트
- 인증: Supabase Auth(구글 OAuth) → 프런트 로그인 → 백엔드 JWT 검증 미들웨어
- 시크릿 관리: NIM API Key, VAPID 키, Supabase 키를 각 배포 환경 시크릿에 등록
- NVIDIA NIM: `build.nvidia.com` 가입 → API Key 발급 → 샘플 호출 1건 성공 확인

**산출물** — 배포된 web/api URL, 로그인 동작, `/health` 200, NIM 호출 성공 로그
**완료 기준(DoD)** — 로그인한 사용자가 빈 대시보드까지 도달하고, 백엔드가 인증된 요청을 구분한다. `git push` → 자동 배포가 동작한다.

---

# M1 · MVP 코어 (F1~F5)

## S1. 영수증 → 재고 등록 (F1)

**목적** — 수기 입력 마찰을 없애는 서비스의 **입구**. 영수증 한 장으로 재고가 채워져야 이후 모든 기능(알림·추천)이 의미를 가진다.

**방법** — 촬영/업로드된 이미지를 Storage에 올리고, **비동기(BackgroundTask)** 로 OCR→정규화→소비기한 매핑을 처리한다. OCR은 오인식이 필연이므로 **자동 등록이 아니라 "사용자 보정 후 확정"** 2단계로 설계해 신뢰를 확보한다.

**세부 작업**
- 프런트: 카메라 촬영(`<input capture>`) / 파일 업로드 UI → `POST /receipts`
- 백엔드 처리 파이프라인(비동기):
  1. 이미지 Supabase Storage 저장, 작업행 생성(`status=processing`)
  2. Nemotron OCR(NIM) 호출 → 원시 품목 텍스트
  3. **품목명 정규화**(핵심 난이도, ↓ 3단계 전략)
  4. 소비기한 DB 매핑 → `expire_at` 계산, `status=done`
- 프런트: `GET /receipts/{id}` 폴링 → **보정 UI**(품목명/수량/카테고리 수정, 항목 삭제/추가)
- 확정: `POST /inventory/confirm` → `inventory_items` 반영

**품목명 정규화 전략 (S1의 성패를 가름)**
- 1차(무료·결정적): 정규식 + 별칭 사전(`item_aliases`)으로 매칭 — `"서울우유1L" → 우유`
- 2차(비용 발생): 1차 실패 항목만 Llama Nemotron에 JSON 정규화 프롬프트로 위임
- 3차(학습): 사용자 보정을 `item_aliases(scope=user)`에 반영 → 본인은 다음부터 1차 해결.
  여러 사용자에서 반복되면 전역 승격(**SD-3**) → 신규 사용자에게도 적용
> 이 순서로 **NIM 호출 수를 최소화**(= 비용 최소화)하면서 정확도를 시간이 지날수록 올린다.
> 이벤트(`item_corrected`)는 **SD-1**로 남겨 적중률 지표(**SD-5**)에 활용.

**구조 스케치(방향만)**
```
inventory_items: id, fridge_id, name, category, qty, unit,
                 purchased_at, expire_at, source, status
item_aliases:    raw_text, normalized_name, hit_count   # 학습 사전
```
**산출물** — 영수증 업로드→보정→재고 등록 동작, 정규화 1·2·3차 파이프라인
**DoD** — 실제 마트 영수증 1장으로 품목 대부분이 인식되고, 사용자가 오인식을 보정해 냉장고에 저장된다. 보정 결과가 별칭 사전에 남아 재사용된다.

---

## S2. 소비기한 추정 (F2)

**목적** — 영수증엔 유통기한이 없다. **"언제까지 먹어야 하나"** 라는 시간 정보를 부여해야 알림·추천이 성립한다. 서비스 가치의 근간.

**방법** — 품목/카테고리별 **기본 소비일수 기준표**를 시딩하고 `구매일 + 기준일수`로 임박일을 계산한다. 기준은 부정확할 수밖에 없으므로 **사용자 개인화 오버라이드**로 보정 가능하게 한다.

**세부 작업**
- `consumption_reference` 시드 데이터 구축(품목/카테고리 → 기본일수)
  - 예: 우유 7 · 시금치 4 · 냉동식품 90 · 통조림 365
- 매핑 규칙: 정확 품목 매칭 → 없으면 카테고리 기본값 → 없으면 전역 기본값
- 개인화: `user_overrides(item, custom_days)` 있으면 우선 적용
- 재고 목록에 **임박 순 정렬** + 임박도 뱃지(D-day) 표시

**구조 스케치**
```
consumption_reference: name|category, default_days
user_overrides:        user_id, item_name, custom_days
# expire_at = purchased_at + (override or item_days or category_days or global)
```
**산출물** — 시드된 기준표, 임박일 계산 로직, 임박 순 재고 화면
**DoD** — 등록된 각 품목에 합리적 `expire_at`이 부여되고, 사용자가 특정 품목 소비기한을 조정하면 즉시 반영된다.

---

## S3. 임박 알림 (F3)

**목적** — **"까먹어서 버린다"** 는 핵심 불만을 직접 때리는 기능. 사용자가 앱을 안 열어도 임박 사실이 **먼저 찾아와야** 낭비가 줄어든다.

**방법** — 상시 스케줄러 없이 **GitHub Actions cron(일 1회)** 이 백엔드 배치 엔드포인트를 호출하고, 해당 시점에 임박 항목을 조회해 **Web Push**로 발송한다($0 유지).

**세부 작업**
- 프런트: 알림 권한 요청 + `push_subscriptions` 등록(`POST /push/subscribe`)
- 온보딩에 **"홈 화면에 추가" 유도**(iOS는 이걸 해야 푸시 도달) — 배너/가이드
- 백엔드: `POST /internal/notify-expiring`(시크릿 토큰 인증)
  - `expire_at - now <= threshold` 항목 조회 → 유저별 그룹핑 → VAPID 푸시 발송
- 인앱 배지/알림함(푸시 못 받는 환경 대비 병행)
- `.github/workflows/notify.yml`: `schedule: cron`으로 매일 엔드포인트 호출

**산출물** — 구독 등록, 배치 엔드포인트, cron 워크플로우, 인앱 알림함
**DoD** — 임박 품목이 있는 사용자가 실제 푸시(또는 최소한 인앱 배지)를 받는다. cron이 매일 자동 실행된다.

> **리스크 메모**: iOS 웹 푸시는 "홈 화면 추가" 필수 + 도달 불안정. 인앱 알림 병행이 안전장치. 헤비유저용 네이티브(Capacitor)는 M3 이후 검토.

---

## S4. 레시피 추천 & 소비 처리 (F4·F5)

**목적** — 임박 재료를 **행동(요리)으로 연결**해 실제로 소진시키는 단계. 알림이 "인지"라면 추천은 "해결". 소비 처리까지 닫아야 재고 정확도가 유지된다.

**방법** — 임박 재료를 우선순위로 두고 보유 재료 전체를 컨텍스트로 Llama Nemotron에 넣어 **구조화 JSON** 레시피를 받는다. 결과는 캐싱해 재호출(비용)을 막는다. 요리 후 사용 재료를 "소비 완료" 처리한다.

**세부 작업**
- `GET /recipes/suggest`: 입력=임박 재료(우선) + 보유 재료 전체
- LLM 프롬프트에 규칙 명시: **"임박 재료를 최대한 소진하는 요리 우선"**, 출력 JSON 스키마 강제
- 응답 검증(스키마 파싱 실패 시 1회 재시도) → 파싱 성공분만 노출
- **캐싱**: (재고 스냅샷 해시 → 추천 결과) 저장, 동일 상태면 재사용
- 레시피 상세 화면(재료·단계) + **"이 요리 만들었어요"** → 사용 재료 `status=consumed`
- F5(재고 목록)와 연결: 소비/폐기 시 상태 갱신, 폐기는 `waste_logs`에 적재(S6 리포트 대비)

**구조 스케치(응답 방향)**
```
GET /recipes/suggest →
[ { title, used_ingredients[], expiring_used[], missing[], steps[] } ]
# expiring_used: 이 요리가 소진하는 임박 재료 (정렬 기준)
```
**산출물** — 추천 API·화면, 소비/폐기 처리, 추천 캐시
**DoD** — 임박 재료가 있을 때 그것을 쓰는 요리가 상위에 추천되고, 요리 후 재고가 줄어든다. **여기까지가 M1: 촬영→등록→알림→추천→소비의 end-to-end 데모 완성.**

---

# M2 · 웹 차별화 (F6~F9)

## S5. 공유 냉장고 (F6)

**목적** — 가족·룸메이트가 **하나의 냉장고**를 공유. 웹의 최대 강점(설치 없이 링크 초대)을 살려 "누가 뭘 넣고 뺐는지" 혼선을 없앤다.

**방법** — 재고를 개인이 아닌 `fridge` 단위에 귀속시키고(설계상 S1부터 `fridge_id` 사용), 멤버십·권한과 **만료형 초대 링크**를 붙인다. 변경 로그로 협업 신뢰를 만든다.

**세부 작업**
- `fridges` / `fridge_members(role: owner|member)` 모델 활성화
- `POST /fridges/{id}/invite` → 만료·권한 포함 토큰 링크 생성, 수락 플로우
- 재고 CRUD에 `fridge_id` 스코프 + 권한 체크(RLS 또는 앱단)
- 변경 로그(추가/소비/폐기, 행위자·시각) 화면
**산출물** — 공유 냉장고 초대·멤버 관리·변경 로그
**DoD** — 두 계정이 같은 냉장고를 보고 편집하며, 초대 링크가 만료·권한을 지킨다.
> **선행 설계 주의**: S1부터 재고를 `fridge_id`에 묶어 두면 S5에서 큰 마이그레이션이 없다. (개인 냉장고 = 멤버 1명인 fridge)

## S6. 절약/낭비 리포트 (F7)

**목적** — 핵심 불만("버린다")을 **수치로 증명**해 효용을 체감시키고 리텐션을 만든다. "이번 달 ○원 아꼈다"가 재방문 이유가 된다.

**방법** — 폐기·소비 이벤트를 누적(`waste_logs`)하고 월 단위로 집계해 시각화한다. 무거운 실시간 집계 대신 조회 시 계산 또는 일 배치 요약.

**세부 작업**
- 소비/폐기 시 로그 적재(품목·추정 금액·시각) — 금액은 영수증 단가 활용
- `GET /reports/waste`: 월별 폐기액/소비액/절약 추정
- 대시보드 차트(데스크톱 강점 활용) + 개선 인사이트("자주 버리는 품목 Top")
**산출물** — 리포트 API·차트
**DoD** — 사용자가 월별 낭비/절약 추이와 자주 버리는 품목을 확인한다.

## S7. 파일 업로드 OCR & 레시피 공유 URL (F8·F9)

**목적** — (F8) 온라인 장보기(PDF·이미지 영수증)까지 커버해 입구를 넓히고, (F9) 생성 레시피를 **공유 가능한 웹페이지**로 만들어 SEO 유입 채널을 확보(앱엔 없는 웹만의 획득 경로).

**방법** — S1 OCR 파이프라인을 파일 업로드 진입점으로 재사용. 레시피에 공개 URL + OG 메타태그를 부여해 SNS/검색 공유 시 미리보기가 뜨게 한다.

**세부 작업**
- 드래그&드롭 업로드(PDF/이미지) → 기존 OCR 파이프라인 재사용
- 레시피 공개 페이지 라우트 + OG 태그(제목·이미지·요약), 사이트맵
- 공유 버튼(링크 복사/SNS)
**산출물** — 파일 업로드 경로, 공유 가능한 레시피 페이지
**DoD** — PDF 영수증이 등록되고, 레시피 링크 공유 시 미리보기 카드가 정상 노출된다.

---

# M3 · 고도화 (F10~F13)

## S8. 음성 입력 (F10)

**목적** — "우유 하나 추가해줘"처럼 **손 안 대고 빠른 등록**. 요리 중·장보기 중 입력 마찰을 더 낮춘다.
**방법** — 브라우저 녹음(`MediaRecorder`) → Parakeet STT(NIM) → Llama Nemotron 의도 파싱 → 재고 반영. **비용 발생 기능이라 무료 한도·캐싱 확인 후 도입.**
**세부 작업** — 녹음 UI → `POST /voice` → STT → 의도(add/remove/qty) 파싱 → 확인 후 반영
**DoD** — 음성 한 마디로 품목이 추가/수정되고, 애매하면 확인 UI로 되묻는다.

## S9. 요리 이미지 생성 & 정규화 학습 루프 (F11~F13)

**목적** — (F11) 시각적 매력 강화, (F13) 정규화 정확도의 **자가 개선**, (F12) 장보기 연동으로 소비-구매 순환 완성.
**방법** — Qwen-Image는 **생성 결과가 실물과 달라 신뢰를 해칠 수 있어** 실제 사진 우선, 생성은 보조로 캐싱 사용. 별칭 사전 학습을 지표화해 1차 정규화 적중률을 올린다.
**세부 작업**
- Qwen-Image(NIM) 대표 이미지 생성 → **결과 캐싱**(재호출 비용 차단), 실제 사진 우선 정책
- `item_aliases` 적중률 대시보드 → 저적중 품목 사전 보강
- 장보기 리스트 자동 생성(소비 패턴 기반) → 마트/배송 API 연동(선택)
**DoD** — 이미지·장보기·학습 지표가 운영 가능한 형태로 붙는다.

---

# 교차 단계 · 데이터 수집 & 거버넌스 (SD)

> [04_데이터수집_PH런칭.md](04_데이터수집_PH런칭.md)의 전략을 **실제 개발 태스크**로 옮긴 것.
> 특정 스프린트에 몰지 않고 **관련 기능 단계에 얹어** 진행한다(착수 시점 표기).
> 공통 원칙: **익명·집계 우선, 동의 기반, 최소 수집.** 개인식별 데이터를 학습에 쓰지 않는다.

## SD-1. 이벤트 트래킹 파이프라인
**목적** — 정규화·추천·소비 등 **개선에 필요한 행동 신호**를 구조적으로 남긴다. 데이터 없이는 플라이휠도 리포트도 없다.
**방법** — 프런트에서 표준 이벤트를 **배치 전송**(`POST /events`)해 `analytics_events`에 적재. 개인식별 없이 `session_id` + 타입 + props만.
**세부 작업**
- 이벤트 규격 정의: `receipt_uploaded`, `item_corrected`(원문→표준), `recipe_suggested`, `recipe_cooked`, `item_discarded` 등
- 프런트 경량 트래커(큐잉+배치 전송), 백엔드 수집 엔드포인트
- 옵트아웃 시 전송 중단(SD-2 연동)
**착수** — S1과 함께 골격, 각 기능 단계에서 이벤트 추가
**DoD** — 핵심 행동이 이벤트로 남고, 옵트아웃 사용자는 수집되지 않는다.

## SD-2. 동의·옵트아웃·영수증 파기 (개인정보 거버넌스)
**목적** — 영수증=결제정보. **신뢰와 법적 안전**을 확보해야 데이터 수집 자체가 지속 가능하다.
**방법** — 온보딩 동의 + 설정에서 토글(`data_consent`, `receipt_retain`). 원본은 품목 추출 후 **cron 배치로 파기**.
**세부 작업**
- 온보딩 동의 화면("익명 데이터로 정확도 개선에 사용") + 개인정보 처리방침 링크
- `PATCH /me/consent`(동의/원본 보관 토글), 옵트아웃 시 SD-1 수집 차단
- `POST /internal/purge-receipts`(cron): `receipt_retain=false` 항목 원본 삭제 → `purged_at` 기록
**착수** — S1(영수증 도입)과 동시 필수
**DoD** — 동의 없이는 수집되지 않고, 보관 미동의 영수증 원본은 파기된다.

## SD-3. 별칭 승격 파이프라인 (개인 → 전역 학습)
**목적** — 사용자 보정을 **전역 정규화 정확도**로 환원(플라이휠 핵심). 단, 오염 방지를 위해 검증 임계를 둔다.
**방법** — 보정 결과는 우선 `scope=user`로 저장. 동일 매핑이 **서로 다른 사용자 N명 이상**에서 반복되면 `scope=global`로 승격.
**세부 작업**
- S1 보정 확정 시 `item_aliases(scope=user)` 기록
- `POST /internal/promote-aliases`(cron): 임계 초과 매핑을 전역 승격(`promoted_at`)
- 정규화 1차 조회 순서: user 사전 → global 사전 → 정규식
**착수** — S1 이후(학습 데이터가 쌓인 뒤), 초기 임계는 보수적으로
**DoD** — 여러 사용자가 같은 표기를 보정하면 전역 사전에 반영되어 신규 사용자도 자동 인식된다.

## SD-4. 검수·관리 도구 (내부 어드민)
**목적** — 부트스트랩 데이터(소비기한·별칭)를 **저비용으로 검수/보강**하고 저품질 학습을 걸러낸다.
**방법** — 별도 화려한 어드민 대신 **최소 내부 도구**(보호된 라우트 또는 Supabase 대시보드 + 쿼리)로 시작.
**세부 작업**
- `consumption_reference` 시드 검수/편집 뷰
- **저적중 별칭 리스트**(hit_count 낮음·미매칭 원문) → 수동 보강 큐
- 전역 승격 후보 검토(선택적 사람 승인)
**착수** — S2(기준표)·S1(별칭) 데이터가 생긴 직후, F13(S9)에서 고도화
**DoD** — 운영자가 자주 틀리는 품목을 찾아 기준표/사전을 보강할 수 있다.

## SD-5. 집계·익명화 & 플라이휠 지표
**목적** — 원시 로그를 **의사결정 가능한 지표**로 만들고, 개인식별을 제거한다.
**방법** — 일 배치로 요약 테이블 생성(정규화 적중률, 자주 버리는 품목 Top, 추천 소진율 등). 리포트(S6)와 검수(SD-4)가 이를 소비.
**세부 작업**
- GitHub Actions cron 배치로 집계 갱신, 개인 필드 제외
- 지표: OCR 정규화 1차 적중률 추이, 품목별 폐기 빈도, 추천→소비 전환율
**착수** — S6(리포트)와 함께
**DoD** — 핵심 지표가 매일 갱신되고 개인정보가 포함되지 않는다.

## SD-6. 대기자 명단 & 랜딩 (프리런칭 데이터)
**목적** — PH 런칭 전 **수요 검증 + 초기 트래픽 풀** 확보. (상세: [04 PART 2](04_데이터수집_PH런칭.md))
**방법** — 정적 랜딩 + 이메일 수집(`POST /waitlist`). 런칭일 예약 발송.
**세부 작업**
- 랜딩 페이지(가치·데모 GIF·이메일 폼), `waitlist` 저장(중복·locale 처리)
- 런칭 안내 발송 수단(무료 이메일 도구) 연동
**착수** — M1 개발과 병행(프리런칭), 런칭 전 완료
**DoD** — 방문자가 대기자로 등록되고, 런칭일에 안내를 받을 수 있다.

---

# 수익화 단계 · 결제 & 구독 게이팅 (S-Billing)

> 상세 정책: [05_수익화_가격정책.md](05_수익화_가격정책.md). **결제 인프라 = Polar 단독(MoR).**
> **착수 시점**: PH 런칭 후 **리텐션(PMF 신호) 검증 뒤** 도입. 초기부터 벽을 세우지 않는다.

## SB-1. 구독 결제 연동 (Polar)
**목적** — 국내+해외 동시 과금 + 글로벌 세금 처리를 최소 운영부담으로 확보($0 지향).
**방법** — Polar(MoR)가 세금·인보이스·환불 대행. 백엔드는 **웹훅으로 구독 상태만 반영**.
**세부 작업**
- Polar 상품·플랜(Pro/Family, 월/연) 구성, 체크아웃 연동
- `subscriptions(user_id, plan, status, renews_at, provider='polar')` 모델
- Polar 웹훅 엔드포인트(생성·갱신·해지·환불) → 구독 상태 동기화
**DoD** — 결제 시 플랜이 활성화되고, 해지·환불이 상태에 정확히 반영된다.

## SB-2. 기능 게이팅 & 사용량 한도
**목적** — 플랜별 기능/한도(§05 요금제)를 **원가 통제 + 전환 압력**으로 작동시킨다.
**방법** — 게이팅 미들웨어가 요청 시 플랜·사용량을 검사. 스캔은 **월 리셋 카운터**.
**세부 작업**
- 플랜별 권한 매핑(스캔 한도·음성·이미지·리포트 범위·공유 인원)
- 월 스캔 카운터(무료 5회) + 리셋 배치
- 한도 초과·유료 기능 접근 시 **페이월 UI**(전환 트리거)
**DoD** — 무료 한도 초과 시 페이월이 뜨고, 결제 후 즉시 해제된다.

---

## 부록 A. 기능 ↔ 목적 ↔ 방법 요약표 (개발자 컨텍스트용)

| 기능 | 목적(왜) | 달성 방법(어떻게) | 단계 |
|------|----------|-------------------|:---:|
| F1 영수증 등록 | 입력 마찰 제거(입구) | OCR→정규화(3단계)→**보정 후 확정** | S1 |
| F2 소비기한 | "언제까지" 시간 부여 | 기준표 + 개인화 오버라이드 | S2 |
| F3 임박 알림 | "까먹음" 방지 | Web Push + GitHub Actions cron | S3 |
| F4 레시피 추천 | 인지→행동 전환 | 임박 우선 LLM(JSON) + 캐싱 | S4 |
| F5 재고/소비 | 재고 정확도 유지 | 상태 전이(active/consumed/discarded) | S4 |
| F6 공유 냉장고 | 웹 강점(무설치 공유) | fridge 스코프 + 만료 초대 링크 | S5 |
| F7 낭비 리포트 | 효용 증명·리텐션 | waste_logs 집계·시각화 | S6 |
| F8 파일 업로드 | 온라인 구매 커버 | S1 파이프라인 재사용 | S7 |
| F9 공유 URL | SEO 유입(획득) | 공개 페이지 + OG 태그 | S7 |
| F10 음성 입력 | 무접촉 빠른 등록 | Parakeet STT + 의도 파싱 | S8 |
| F11 이미지 생성 | 시각 매력 | Qwen-Image + 캐싱(사진 우선) | S9 |
| F13 정규화 학습 | 정확도 자가 개선 | 별칭 사전 피드백 루프 | S9 |
| SD-1 이벤트 트래킹 | 개선용 행동 신호 확보 | 익명 이벤트 배치 수집 | S1~ |
| SD-2 동의·파기 | 개인정보 신뢰·안전 | 동의 토글 + 원본 파기 배치 | S1 |
| SD-3 별칭 승격 | 개인 보정→전역 정확도 | 임계 기반 user→global 승격 | S1~ |
| SD-4 검수 도구 | 부트스트랩 데이터 보강 | 최소 어드민·저적중 큐 | S2~ |
| SD-5 집계·지표 | 의사결정·익명화 | 일 배치 요약 테이블 | S6 |
| SD-6 대기자/랜딩 | 프리런칭 수요·유입 | 랜딩 + waitlist | 병행 |

## 부록 B. 공통 개발 규칙
- **비용 가드레일**: 모든 NIM 호출 앞에 캐시 조회 → 히트 시 호출 생략. 호출 수/토큰 로깅.
- **LLM 응답**: 반드시 JSON 스키마 검증, 실패 1회 재시도 후 폴백(빈 결과·안내).
- **개인정보**: 영수증 원본은 품목 추출 후 파기 옵션, Storage 비공개+서명 URL.
- **완료 정의**: 각 단계 DoD를 만족해야 다음 단계 착수(M1은 특히 순차 의존).
- **테스트 우선순위**: 정규화(S1)·소비기한 계산(S2)·추천 파싱(S4)은 단위 테스트 필수.
