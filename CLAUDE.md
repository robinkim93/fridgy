# Fridge Guardian AI

냉장고 식재료 유통기한 관리 + 임박 재료 레시피 추천 서비스.
**PWA(스토어 업로드 불필요) · 운영비용 $0 지향 · 국내+해외 타깃.**

기획/설계 문서(맥락이 필요할 때만 해당 파일을 연다):
- [01_기획서.md](01_기획서.md) · [02_기술아키텍처.md](02_기술아키텍처.md) · [03_개발계획.md](03_개발계획.md)
- [04_데이터수집_PH런칭.md](04_데이터수집_PH런칭.md) · [05_수익화_가격정책.md](05_수익화_가격정책.md)

---

## 1. Git 워크플로우 (반드시 준수)

브랜치 흐름: **main > dev > feature**

1. **main 브랜치는 명시적 요청이 없는 한 절대 건드리지 않는다.**
   - main으로의 commit/merge/push는 금지. `.claude/hooks/protect-main.sh`(PreToolUse hook)가 강제 차단한다.
   - 사용자가 명시적으로 main 변경을 요청한 경우에만, 승인 후 명령 앞에 `FG_ALLOW_MAIN=1` 을 붙여 우회한다.
2. **작업은 항상 dev에서 파생한 feature 브랜치에서 진행한다.**
   - 새 작업 시작: `git switch dev && git switch -c feature/<작업명>`
   - 이름 규칙: `feature/<간단한-작업설명>` (예: `feature/receipt-ocr`)
3. **작업 후 원격 dev 업로드는 사용자 의사를 먼저 묻는다.**
   - 커밋을 마치면 "원격 dev에 push할까요?"를 반드시 묻고, 답변대로만 진행한다.
   - 승인 시에만 push. 거절 시 로컬 커밋만 유지.
4. 커밋/푸시는 사용자가 요청·승인했을 때만 수행한다(임의로 하지 않는다).

---

## 2. 모델 라우팅 (토큰 절감의 핵심 레버)

**단순·반복 작업은 Haiku 모델로 처리한다.** 추론이 필요 없는 기계적 작업에 고가 모델을 쓰지 않는다.

### Haiku로 처리 (기본)
- **Git 작업**: add/commit/push, 브랜치 생성·전환, status·log 확인, diff 요약
- **데이터 수집**: 소비기한/별칭 시드 생성, 영수증 샘플 정리, 크롤링·포맷 변환
- **파일 읽기/탐색**: 파일 열람, grep/glob 검색, 로그·출력 확인
- **정형 변환**: JSON 정리, 표 생성, 이름 규칙 일괄 적용, 보일러플레이트 생성
- 실행 방법: 이런 작업은 **Haiku 서브에이전트로 위임**하거나, 세션 모델을 Haiku로 두고 진행한다.

### Opus/Sonnet로 처리 (예외)
- 아키텍처 설계, 정규화·추천 로직 등 판단이 필요한 코드, 리뷰, 트레이드오프 결정, 이 문서류 기획.

> 원칙: **"생각이 필요하면 Opus, 손이 필요하면 Haiku."**

---

## 3. 토큰 사용량 절감 방안 (목표 70%+ 절감)

아래를 기본 습관으로 삼는다. 절감 효과가 큰 순서.

### 3.1 모델 라우팅 (가장 큰 절감)
- §2대로 반복 작업을 Haiku로 내리면 동일 작업의 토큰 단가가 크게 낮아진다. **전체 절감의 대부분이 여기서 나온다.**

### 3.2 읽기 최소화
- 파일 전체를 통째로 읽지 않는다. **Grep/Glob으로 위치를 먼저 찾고**, `offset`/`limit`로 필요한 구간만 읽는다.
- **방금 편집한 파일을 재확인차 다시 읽지 않는다**(하네스가 상태를 추적하므로 불필요).
- 같은 내용을 반복 로드하지 않는다. 기획 문서는 **경로 링크로 참조**하고 본문을 붙여넣지 않는다.

### 3.3 서브에이전트로 컨텍스트 격리
- 광범위한 탐색(여러 디렉토리/네이밍 스윕)은 **Explore 서브에이전트**에 맡긴다.
  원시 파일 덤프는 서브에이전트 컨텍스트에 남고, 메인에는 **결론만** 돌아와 토큰을 아낀다.
- 대량 검색이 불확실할 때 직접 여러 번 읽지 말고 에이전트에 위임한다.

### 3.4 도구 사용 효율
- 의존성 없는 도구 호출은 **한 번에 병렬 실행**한다(왕복 축소).
- 셸 출력은 좁힌다: `git diff --stat`, 경로 지정 diff, `head`/필터로 대용량 출력 방지.
- 장시간/대량 출력 명령은 필요한 필드만 뽑아 반환한다.

### 3.5 결과 캐싱·재사용
- 데이터 수집·생성 결과는 **파일/DB에 저장**하고 재생성하지 않는다.
- 반복 참조되는 사실은 **memory/CLAUDE.md**에 기록해 매번 재유도하지 않는다.

### 3.6 응답·컨텍스트 관리
- 응답은 간결하게. 이미 확정된 사실을 재설명하지 않는다.
- 긴 세션은 적절히 compact하여 불필요한 이력 누적을 줄인다.

---

## 4. 프로젝트 상수 (반복 확인 불필요)

- **스택**: React+Vite PWA(Cloudflare Pages) · FastAPI(Fly.io) · Supabase(DB/Auth/Storage) · NVIDIA NIM(AI) · Polar(결제)
- **비용**: NIM 무료 티어 + 결과 캐싱, 자체 GPU 없음. Parakeet·Qwen-Image는 MVP 제외.
- **요금제**: Free / Pro ₩3,900 / Family ₩6,900. 구독 단독. 유료화는 PH 런칭·PMF 이후.
- **개인정보**: 영수증 원본은 품목 추출 후 파기 옵션. 익명·집계 데이터만 학습.

이 상수들은 문서에 이미 확정되어 있으니, 관련 작업 시 문서를 다시 통독하지 말고 이 요약을 활용한다.
