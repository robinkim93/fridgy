# Fridge Guardian AI — 기술 아키텍처 & 스택 설계

- 문서 버전: v1.1
- 작성일: 2026-07-01 (개정: 2026-07-02 — 무료 티어 스택 확정)
- 관련 문서: [01_기획서.md](01_기획서.md)
- **제약: 운영 비용 $0 목표** (전 계층 무료 티어 + NVIDIA NIM 무료 티어)

---

## 1. 아키텍처 개요

PWA 프런트엔드 + API 서버 + AI 추론 계층으로 구성한다.
모든 계층은 **무료 티어 서비스**로 배치하고, NVIDIA 오픈 모델은 **NVIDIA NIM(build.nvidia.com) 무료 티어**로 호출한다(자체 GPU 서빙 없음).

```
┌─────────────────────────────────────────────────────────┐
│                Client (PWA) — Cloudflare Pages           │
│  React + Vite + TS  │  Service Worker  │  IndexedDB       │
│  - 카메라/파일 업로드   - 오프라인 캐시     - 로컬 재고 캐시    │
│  - 음성 녹음          - Web Push 수신                       │
└───────────────┬─────────────────────────────────────────┘
                │ HTTPS / REST(+SSE)
┌───────────────▼─────────────────────────────────────────┐
│              API Server (Backend) — Fly.io               │
│  FastAPI (Python) + BackgroundTasks                      │
│  - Auth / 세션      - 재고 CRUD      - AI 오케스트레이션     │
│  - 품목 정규화       - Web Push 발송   - 결과 캐싱           │
└──────┬───────────────────┬───────────────┬───────────────┘
       │                   │               │
┌──────▼──────┐   ┌────────▼───────┐  ┌────▼──────────────┐
│  Supabase   │   │  AI Inference  │  │  Cron Trigger     │
│ - Postgres  │   │  (NVIDIA NIM)  │  │  GitHub Actions   │
│ - Auth      │   │  - Nemotron OCR│  │  (일 1회 스케줄)    │
│ - Storage   │   │  - Llama Nemo. │  │  → 임박 알림 배치   │
│ (영수증 이미지)│   │  (Parakeet/    │  │    엔드포인트 호출  │
│             │   │   Qwen: 후순위) │  │                   │
└─────────────┘   └────────────────┘  └───────────────────┘
```

> **$0 설계 포인트**: 별도 Celery 워커·상시 Redis·GPU 노드를 두지 않는다.
> 장시간 작업은 FastAPI `BackgroundTasks` + DB 작업상태로 처리하고,
> 주기 배치(임박 알림)는 **GitHub Actions cron**이 백엔드 엔드포인트를 호출해 대체한다.

---

## 2. 기술 스택 (확정 — 무료 티어)

> 선정 기준: ① 무료 티어로 $0 운영 가능 ② 이동 부품(운영 요소) 최소화 ③ 확장 시 유료 전환 경로 존재.
> "확장 시" 열은 트래픽이 무료 한도를 넘었을 때의 전환 방향으로, **지금 구축하지 않는다.**

### 2.1 프런트엔드 (PWA)
| 항목 | 확정 선택 | 이유 / 무료 티어 |
|------|-----------|------------------|
| 프레임워크 | React + TypeScript | 생태계·PWA 지원 성숙 |
| 빌드 | Vite + `vite-plugin-pwa` | manifest·SW 자동 생성 |
| 상태관리 | TanStack Query + Zustand | 서버/로컬 상태 분리 |
| UI | Tailwind CSS + shadcn/ui | 반응형 빠른 구축, 무료 |
| 오프라인 | Service Worker + IndexedDB(Dexie) | 재고 캐시·오프라인 조회 |
| 알림 | Web Push API (VAPID) | 키 자체 생성, **비용 $0** |
| **호스팅** | **Cloudflare Pages** | 정적 배포 무료·글로벌 CDN |

### 2.2 백엔드
| 항목 | 확정 선택 | 이유 / 무료 티어 | 확장 시 |
|------|-----------|------------------|---------|
| 언어/프레임워크 | Python + FastAPI | AI 라이브러리 친화, async | — |
| 비동기 작업 | **FastAPI `BackgroundTasks`** + DB 작업상태 | 별도 워커·브로커 불필요 → $0 | Celery + Redis(Upstash) |
| 주기 배치 | **GitHub Actions cron** → 엔드포인트 호출 | 상시 스케줄러 불필요, 무료 | Fly.io Machines cron |
| DB | **Supabase (PostgreSQL)** | 관계형 + 무료 500MB | Neon / 유료 Supabase |
| 인증 | **Supabase Auth** (OAuth 포함) | 구글 로그인·세션 내장, 무료 | — |
| 오브젝트 스토리지 | **Supabase Storage** | 영수증/이미지, 무료 1GB | Cloudflare R2 |
| **호스팅** | **Fly.io** (무료 소형 머신) | 상시 API, 무료 티어 | 유료 스케일업 |

> Supabase로 **DB·Auth·Storage를 한 곳**에 묶어 이동 부품과 연동 비용을 줄인다.

### 2.3 AI 추론 계층 — **NVIDIA NIM 무료 티어 단일화**
| 모델 | MVP 사용 | 서빙(확정) | 인터페이스 |
|------|:---:|-----------|-----------|
| Nemotron OCR v2 | ✅ | **NVIDIA NIM (build.nvidia.com)** | 이미지 → 구조화 텍스트 |
| Llama Nemotron | ✅ | **NVIDIA NIM** | 프롬프트 → 추론(JSON) |
| Parakeet-TDT | ⏸ 후순위 | NVIDIA NIM | 오디오 → 텍스트(STT) |
| Qwen-Image | ⏸ 후순위 | NVIDIA NIM | 프롬프트 → 이미지 |

> **자체 GPU 서빙 없음** — GPU 인스턴스는 $0이 아니므로 채택하지 않는다.
> 전량 NIM 무료 티어로 호출하고, **결과를 DB에 캐싱**해 호출 수를 최소화한다.
> 무료 한도 초과 시에만 vLLM 자체 서빙을 검토(확장 단계).
> 비용·부하가 큰 **Parakeet·Qwen-Image는 MVP에서 제외**한다.

---

## 3. 핵심 파이프라인 설계

### 3.1 영수증 → 재고 등록 (F1)

```
[1] 클라이언트: 이미지 촬영/업로드 → POST /receipts (multipart)
[2] 서버: Supabase Storage 저장, 작업행 생성(status=processing) → job_id 반환(202)
[3] BackgroundTask: Nemotron OCR(NIM) 호출 → 원시 품목 리스트
[4] BackgroundTask: 품목명 정규화 (매핑 테이블 + Llama Nemotron 보조)
        "서울우유1L" → {name:"우유", qty:1, unit:"L", category:"유제품"}
[5] BackgroundTask: 소비기한 DB 매핑 → expire_at 계산, 작업행 status=done
[6] 클라이언트: GET /receipts/{id} 폴링으로 결과 수신
[7] 클라이언트: 사용자 보정 UI → 확정 POST /inventory/confirm
```

**정규화 전략 (가장 어려운 부분)**
- 1차: 정규식·별칭 사전(매핑 테이블)으로 결정적 매칭
- 2차: 실패 항목만 Llama Nemotron에 "품목명 정규화" 프롬프트로 위임
- 3차: 사용자가 보정한 결과를 별칭 사전에 **학습 반영**(피드백 루프)

### 3.2 소비기한 추정 (F2)
- `consumption_reference` 테이블: 품목/카테고리별 기본 소비일수
  - 예: 우유 7일, 시금치 4일, 냉동식품 90일, 통조림 365일
- `expire_at = purchased_at + reference_days`
- 사용자 보정값은 **개인화 오버라이드**로 우선 적용

### 3.3 임박 알림 (F3)
- **GitHub Actions cron**(일 1회)이 `POST /internal/notify-expiring` 호출 (시크릿 토큰 인증)
- 엔드포인트가 `expire_at - now <= threshold` 항목 조회
- 대상 유저에게 Web Push 발송(VAPID) + 인앱 배지 갱신
- iOS는 홈화면 추가된 경우만 도달 → 온보딩에서 유도
- 별도 상시 스케줄러(Celery Beat) 불필요 → **$0 유지**

### 3.4 레시피 추천 (F4)
```
입력: 임박 재료(우선순위) + 보유 재료 전체
  ↓
Llama Nemotron 프롬프트 (JSON 스키마 강제)
  ↓
출력: [{요리명, 사용 재료, 임박재료 소진율, 레시피 단계}]
  ↓
(선택) Qwen-Image로 대표 이미지 생성 → 캐시
```
- 프롬프트에 "임박 재료를 최대한 소진하는 요리 우선" 규칙 명시
- 응답은 **구조화 JSON** 으로 받아 파싱 안정성 확보

### 3.5 음성 입력 (F10)
```
MediaRecorder(webm/opus) → POST /voice → Parakeet STT
  → Llama Nemotron 의도 파싱("우유 하나 추가" → add(우유,1))
  → 재고 반영
```

---

## 4. 데이터 모델 (초안)

```
users(id, email, oauth_provider, created_at,
  data_consent(bool), receipt_retain(bool))       -- 데이터 사용 동의·원본 보관(SD-2)

fridges(id, name, owner_id)                       -- 공유 냉장고 단위
fridge_members(fridge_id, user_id, role)          -- 공유(F6)

inventory_items(
  id, fridge_id, name, category, quantity, unit,
  purchased_at, expire_at, source(receipt|voice|manual),
  status(active|consumed|discarded), created_at)

receipts(id, fridge_id, image_url, ocr_status, raw_json,
  purged_at, created_at)                          -- 원본 파기 시각(SD-2)

consumption_reference(id, name, category, default_days)   -- 소비기한 DB
item_aliases(                                             -- 정규화 학습(SD-3)
  id, raw_text, normalized_name, hit_count,
  scope(user|global), owner_id, promoted_at)             -- 개인→전역 승격
user_overrides(user_id, item_name, custom_days)           -- 개인화

recipes(id, title, ingredients_json, steps_json, image_url, created_at)

waste_logs(id, fridge_id, item_name, cost, wasted_at)     -- 리포트(F7)

push_subscriptions(id, user_id, endpoint, keys_json)      -- Web Push

-- 데이터 수집(SD)
analytics_events(id, user_id?, session_id, event_type,    -- 익명 이벤트 트래킹
  props_json, created_at)        -- 예: recipe_suggested / recipe_cooked / item_corrected
waitlist(id, email, source, locale, created_at)           -- 프리런칭 대기자(SD-6)
```
> `user_id?` = 옵트아웃/비로그인 시 NULL(세션 단위 익명). 개인식별 없는 **패턴만** 학습에 사용.

---

## 5. 주요 API (초안)

| 메서드 | 경로 | 설명 |
|--------|------|------|
| POST | `/receipts` | 영수증 업로드 → OCR 작업 시작 |
| GET | `/receipts/{id}` | OCR 진행/결과 조회 |
| POST | `/inventory/confirm` | 보정된 품목 확정 등록 |
| GET | `/inventory` | 재고 목록(임박 순 정렬) |
| PATCH | `/inventory/{id}` | 수량/상태(소비·폐기) 변경 |
| GET | `/recipes/suggest` | 임박 재료 기반 레시피 추천 |
| POST | `/voice` | 음성 입력 처리 |
| POST | `/fridges/{id}/invite` | 공유 냉장고 초대 링크 |
| GET | `/reports/waste` | 절약/낭비 리포트 |
| POST | `/push/subscribe` | Web Push 구독 등록 |
| POST | `/events` | 익명 이벤트 수집(배치 전송, SD-1) |
| PATCH | `/me/consent` | 데이터 사용 동의·원본 보관 토글(SD-2) |
| POST | `/waitlist` | 프리런칭 대기자 등록(SD-6) |
| POST | `/internal/promote-aliases` | 별칭 승격 배치(cron, SD-3) |
| POST | `/internal/purge-receipts` | 원본 파기 배치(cron, SD-2) |

---

## 6. 인프라 & 배포 (확정 — 무료 티어)

| 항목 | 확정 선택 | 무료 여부 | 확장 시 |
|------|-----------|:---:|---------|
| 프런트 배포 | Cloudflare Pages | ✅ | — |
| 백엔드 배포 | Fly.io (소형 머신) | ✅ | 유료 스케일업 |
| DB/Auth/Storage | Supabase | ✅ | Neon·R2 분리 |
| AI 서빙 | NVIDIA NIM (build.nvidia.com) | ✅(무료 한도) | vLLM 자체 서빙 |
| 주기 배치 | GitHub Actions cron | ✅ | Fly Machines cron |
| CI/CD | GitHub Actions | ✅ | — |
| 관측 | 구조화 로깅 + Sentry(무료 티어) | ✅ | 유료 APM |
| 로컬 개발 | Docker Compose (Supabase 로컬 + FastAPI) | ✅ | — |

---

## 7. 보안 & 개인정보
- 영수증은 결제정보 포함 → **품목만 추출 후 원본 즉시 파기 옵션** 제공
- 오브젝트 스토리지 비공개 버킷 + 서명 URL
- 공유 냉장고 초대 링크는 만료·권한(role) 기반
- Web Push 키(VAPID)·OAuth 시크릿은 시크릿 매니저 관리

---

## 8. 개발 단계 (Phase별 기술 과제)

> 상세한 태스크 단위 실행 계획은 [03_개발계획.md](03_개발계획.md) 참고.

**Phase 1 (MVP)**
- PWA 스캐폴딩(카메라·SW·manifest)
- FastAPI + Supabase(Postgres/Auth/Storage) 기본 골격
- Nemotron OCR(NIM) 연동 + 품목 정규화 v1(매핑 테이블)
- 소비기한 DB 시딩 + 임박 알림(Web Push + GitHub Actions cron)
- Llama Nemotron 레시피 추천(JSON 스키마)

**Phase 2 (웹 차별화)**
- 공유 냉장고(멤버/권한/초대), 변경 로그
- 낭비 리포트 집계 배치
- 파일 업로드(PDF) OCR, 레시피 공유 URL(+OG 태그/SEO)

**Phase 3 (고도화)**
- Parakeet 음성 입력, Qwen-Image 생성 캐시
- 별칭 사전 학습 루프 고도화, 장보기 API 연동

---

## 9. 기술 리스크
| 리스크 | 대응 |
|--------|------|
| OCR 정규화 정확도 | 매핑 테이블 + LLM 보조 + 사용자 피드백 학습 |
| LLM 응답 비정형 | JSON 스키마 강제·검증, 실패 시 재시도 |
| GPU 비용 | NIM(초기) → 자체 서빙, 결과 캐시 적극 활용 |
| iOS 푸시 제약 | 홈화면 추가 유도, 인앱 알림 병행 |
